UPDATE COLUMNS_VISIBILITY SET COLUMNS_VISIBILITY.COLUMNS_NUMBERS = 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 0, 2+3*5) + '0, ' + 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 2+3*5, LEN(COLUMNS_VISIBILITY.COLUMNS_NUMBERS))
WHERE COLUMNS_VISIBILITY.TABLES_NAMES = 
'serv_contract_stage_vo_for_ocp+OCP_VO'

UPDATE COLUMNS_VISIBILITY SET COLUMNS_VISIBILITY.COLUMNS_NUMBERS = 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 0, 2+3*16) + '0, ' + 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 2+3*16, LEN(COLUMNS_VISIBILITY.COLUMNS_NUMBERS))
WHERE COLUMNS_VISIBILITY.TABLES_NAMES = 
'work_plan_vo+OCP_VO'

UPDATE COLUMNS_VISIBILITY SET COLUMNS_VISIBILITY.COLUMNS_NUMBERS = 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 0, 2+3*12) + '0, ' + 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 2+3*12, LEN(COLUMNS_VISIBILITY.COLUMNS_NUMBERS))
WHERE COLUMNS_VISIBILITY.TABLES_NAMES = 
'ocp_outer+OCP_VO'

UPDATE COLUMNS_VISIBILITY SET COLUMNS_VISIBILITY.COLUMNS_NUMBERS = 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 0, 2+3*12) + '0, ' + 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 2+3*12, LEN(COLUMNS_VISIBILITY.COLUMNS_NUMBERS))
WHERE COLUMNS_VISIBILITY.TABLES_NAMES = 
'ocp_service+OCP_VO'

UPDATE COLUMNS_VISIBILITY SET COLUMNS_VISIBILITY.COLUMNS_NUMBERS = 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 0, 2+3*11) + '0, ' + 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 2+3*11, LEN(COLUMNS_VISIBILITY.COLUMNS_NUMBERS))
WHERE COLUMNS_VISIBILITY.TABLES_NAMES = 
'ig_ocp_pki+OCP_VO'

UPDATE COLUMNS_VISIBILITY SET COLUMNS_VISIBILITY.COLUMNS_NUMBERS = 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 0, 2+3*17) + '0, ' + 
SUBSTRING(COLUMNS_VISIBILITY.COLUMNS_NUMBERS, 2+3*17, LEN(COLUMNS_VISIBILITY.COLUMNS_NUMBERS))
WHERE COLUMNS_VISIBILITY.TABLES_NAMES = 
'ocp_trip_vo+OCP_VO'